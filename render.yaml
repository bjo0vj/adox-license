services:
  - type: web
    name: adoexport-license
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_USER
        value: bjo0vj
      - key: ADMIN_PASS
        value: Phat@0833
      - key: DB_PATH
        value: /opt/render/project/src/license.db
